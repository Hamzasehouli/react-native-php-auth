import React from "react";

import { StyleSheet } from "react-native";

export default StyleSheet.create({
  primaryColor: "#9796F0",
  secondaryColor: "#FBC7D4",
  darkColor: "grey",
  lightColor: "white",
});
